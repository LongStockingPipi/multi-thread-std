什么是线程安全？
答：
当多个线程访问一个对象的时候，如果
1. 不考虑这些线程在运行时环境下的调度和交替执行
2. 不需要额外的同步
3， 调用方不需要进行任何其它的协调操作
调用这个对象的行为都可以获得正确的结果，那么这个对象是线程安全的。
即：编写业务逻辑的时候无需做任何额外的处理（像单线程一样编程），程序也可以正常运行，就可以称为线程安全。


死锁

多个线程相互持有对方所需要的资源，又不主动释放，导致所有的线程都无法继续执行，导致程序陷入无尽阻塞，这就是死锁。